# Catalogo di Pillole

Elenco degli spezzoni YouTube che l'app Pillole scarica a ogni apertura.
Non modificare `spezzoni.json` a mano: si genera con `strumenti/spezzoni.py`
e si pubblica con `strumenti/pubblica.sh`.
